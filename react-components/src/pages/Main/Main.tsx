import React, { Component } from 'react';

const BASE_PATH: string = 'https://rickandmortyapi.com/api';
const SEARCH_PATH: string = '/search';
const SEARCH_PARAM: string = 'query=';

class Main extends Component<Record<string, never>, Record<string, never> }> {



  render() {
    return (
      <>
        {/*<SearchBar />*/}
      </>
    );
  }
}

export default Main;
